# 文本 LLM（仅用于把审核后的 OCR 文本结构化为题目 JSON，不做视觉识别）
# 任何 OpenAI 兼容接口都可以；复制本文件为 .env 后填写。
LLM_BASE_URL=https://api.example.com/v1
LLM_API_KEY=
LLM_MODEL=your-model-name

# 单次结构化请求的最大文本字符数（超出自动分块后合并）
STRUCTURE_CHUNK_CHARS=6000

# 上传保护：默认 100 MiB、500 页
OCR_MAX_UPLOAD_BYTES=104857600
OCR_MAX_PAGES=500
